const colors = require('tailwindcss/colors');

module.exports = {
  theme: {
    extend: {
      colors: {
        cyan: colors.cyan,
        sky: colors.sky,
        slate: colors.slate,
      },
      
    },
  },
  plugins: [],
};